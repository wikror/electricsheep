package app;

import javafx.event.ActionEvent;
import javafx.scene.control.Button;

public class MainScreenController {
    /* The initial button at the main screen: */
    public Button startButton;

    /* Invoked after startButton clicked, runs the fileChooser & proceeds with the study: */
    public void chooseFile(ActionEvent actionEvent) {
        //TODO WILL DO FILE CHOOSING TOMORROW GOT THE CLASS ALREADY: javafx.stage.FileChooser
    }
}
